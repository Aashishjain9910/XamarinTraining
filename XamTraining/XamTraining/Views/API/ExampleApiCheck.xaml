<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XamTraining.Views.API.ExampleApiCheck">
    <ContentPage.Content>
        <StackLayout>
            <CollectionView x:Name="colList"  VerticalOptions="Start" HorizontalScrollBarVisibility="Never" SelectionMode="Single" >
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Horizontal" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout  VerticalOptions="Center" Margin="0,0,13,0" >
                            <Label Text="{Binding humidity}" HorizontalTextAlignment="Center" FontSize="20"/>
                            <Label Text="{Binding pressure}" HorizontalOptions="Center" />
                            <Label Text="{Binding dt}" HorizontalTextAlignment="Center" FontSize="20"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>