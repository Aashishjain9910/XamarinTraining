<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:Controls="clr-namespace:XamTraining"
             mc:Ignorable="d"
             x:Class="XamTraining.Views.FloatingEntry">
    <ContentView.Content>
        <Frame x:Name="frame" Margin="1" Padding="0.2">
            <StackLayout Orientation="Horizontal" Spacing="0" Padding="0" Margin="0">
                <Frame Padding="5" Margin="0" VerticalOptions="Center" BackgroundColor="White">
                    <Image x:Name="img" />
                </Frame>
                <StackLayout Spacing="0" Padding="0" VerticalOptions="Center" Margin="0">
                    <Label x:Name="HiddenLabel" FontSize="16" Margin="0" Padding="0" BackgroundColor="AliceBlue" IsVisible="False"/>
                    <Controls:CustomEntry x:Name="EntryField" Text="{Binding Text,Mode=TwoWay}" VerticalOptions="Center" Margin="0" WidthRequest="300" BackgroundColor="Yellow"/>
                </StackLayout>
            </StackLayout>
        </Frame>
    </ContentView.Content>
</ContentView>
    